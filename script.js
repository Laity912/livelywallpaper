   function change(color)
    {
        document.documentElement.style.setProperty("--myvar", color);
        document.documentElement.style.setProperty("--myborder", color);

        
    }   
     function myborder(color)
     {
         document.documentElement.style.setProperty("--myborder", color);
        
     }   
    
